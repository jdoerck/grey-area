{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep -q 'glyph-table.tsv' && (cd /Users/jdoerck/Documents/Repos/marain && python3 direction/scripts/generate-glyph-table.py) || true"
          }
        ]
      }
    ]
  }
}
